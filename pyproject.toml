[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "tetrisEngine-rs"
version = "0.1.0"
requires-python = "==3.13.12"

[tool.maturin]
bindings = "pyo3"
features = ["python"]
module-name = "tetrisEngine_rs"
